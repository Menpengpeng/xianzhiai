# 闲值 AI 浏览器插件：产品需求与原型设计说明

版本：V0.1（产品设计稿）  
日期：2026-09-20  
范围：纯浏览器扩展，无自建后端

## 1. 产品定义

“闲值 AI”是一款运行在闲鱼 Web 商品页上的 AI 辅助浏览器插件。插件自动读取当前商品页中用户已经能看到的标题、描述、价格、成色、类目、图片数量与卖家公开信息，由用户选择的模型提供商生成估价建议、价格区间、影响因素和交易建议。

插件不建设业务后端，不托管用户 API Key，不代替闲鱼执行购买、聊天或发布操作。所有模型请求均由浏览器直接发往用户配置的模型提供商。

> 产品口径：结果是 AI 建议，不是实时成交价或鉴定结论。若没有可信的同类商品样本，必须明确降低置信度，禁止伪造“历史成交数据”。

## 2. 目标与非目标

### 2.1 产品目标

1. 用户在商品详情页一键获得“合理价格区间 + 建议价 + 依据”。
2. 首次配置不超过 3 分钟，后续估价主路径不超过 2 次点击。
3. 默认支持 DeepSeek、通义千问（Qwen）、智谱 GLM、火山引擎方舟，并允许新增 OpenAI 兼容提供商。
4. 视觉与闲鱼当前 Web 页面融合，但持续显示“AI 助手 / 非闲鱼官方”，避免误认为站内原生能力。
5. API Key 只保存在本地扩展存储中，请求不经过第三方中转服务。

### 2.2 非目标

- 不做账号体系、云同步、团队管理或服务端计费。
- 不自动登录闲鱼，不读取私信，不代替用户发送消息、下单或发布商品。
- V1 不承诺全网实时比价，不承诺商品真伪鉴定。
- V1 不批量抓取商品或绕过站点限制。

## 3. 目标用户与核心场景

### 买家

- 在购买二手数码、家电、潮玩前判断挂牌价是否合理。
- 快速发现描述缺失、维修史、型号矛盾等风险点。
- 生成礼貌且有依据的议价话术，手动复制到聊天框。

### 卖家

- 根据商品成色与配置得到建议挂牌区间。
- 优化标题和商品描述，补齐关键信息。
- 在“快速出手 / 均衡 / 尽量卖高”三个目标间选择定价策略。

## 4. 页面入口与信息架构

### 4.1 页面入口

沿用参考页右下角已有的胶囊形入口位置，显示黄色“闲”字图标和“AI 估价”。入口固定在页面右下侧，但与闲鱼右侧原生工具条保持 16px 间距，不覆盖“聊一聊”“立即购买”等关键动作。

- 默认：`闲 AI 估价`
- 分析中：`正在估价…`
- 已有结果：`¥980 建议价`
- 不支持页面：入口隐藏

### 4.2 侧栏结构

侧栏建议宽度 400px，最大不超过可视区域宽度的 42%；窄屏时改为底部抽屉。结构从上到下为：

1. 品牌头部：黄色“闲”字图标、产品名、“非闲鱼官方”、关闭与设置。
2. 商品识别卡：缩略图、标题、页面价格、抓取状态。
3. 提供商选择：显示当前提供商与模型，支持快捷切换。
4. 主操作：开始估价 / 重新估价。
5. 结果区：建议价、区间、置信度、价格判断、关键依据、风险与建议。
6. 次操作：复制议价话术、优化文案、反馈结果。
7. 页脚：AI 建议免责声明与隐私入口。

## 5. 关键用户流程

### 5.1 首次使用

1. 用户打开闲鱼商品详情页并点击“闲 AI 估价”。
2. 插件识别尚未配置提供商，展示单页配置引导。
3. 用户选择 DeepSeek / Qwen / GLM / 火山引擎。
4. 用户粘贴 API Key；插件提供“去获取 API Key”外链和“仅保存在本机”说明。
5. 用户点击“测试连接”。成功后保存并自动返回当前商品估价。
6. 若失败，错误信息必须区分 Key 无效、余额不足、网络/CORS、模型名错误和频率限制。

### 5.2 日常估价

1. 点击入口，侧栏立即显示识别到的商品摘要。
2. 用户点击“开始估价”。
3. 插件展示阶段状态：正在读取商品 → 正在请求模型 → 正在整理结果。
4. 成功后先呈现建议价与区间，再展示依据和风险；保留“重新估价”。
5. 用户可复制结果或议价话术；插件不自动发送。

### 5.3 切换提供商

1. 在结果页点击当前提供商标签或右上角设置。
2. 选择另一个已经配置好的提供商后立即生效。
3. 未配置的提供商显示“待配置”，点击进入对应配置表单。
4. 切换后不自动产生付费请求；必须由用户再次点击“重新估价”。

## 6. 提供商配置设计

默认预置的是“连接模板”，不是平台密钥。用户必须使用自己的 API Key。

| 提供商 | UI 名称 | 默认能力 | 必填项 | 说明 |
|---|---|---|---|---|
| DeepSeek | DeepSeek | 估价、文案、风险分析 | API Key、模型 | 默认首选，OpenAI 兼容请求结构 |
| 阿里云百炼 | 通义千问 Qwen | 估价、文案、图文理解（模型支持时） | API Key、模型 | 使用兼容模式或官方 SDK 约定 |
| 智谱 AI | GLM | 估价、文案、图文理解（模型支持时） | API Key、模型 | 使用其开放平台接口 |
| 火山引擎方舟 | 火山引擎 | 估价、文案、图文理解（接入点支持时） | API Key、Endpoint/模型 | 允许填写推理接入点 ID |
| 自定义 | OpenAI 兼容 | 取决于模型 | Base URL、API Key、模型 | 用于兼容其他厂商或本地网关 |

配置字段：`providerId`、显示名、Base URL、API Key、模型名、超时时间、温度、是否启用图片。高级字段默认折叠，避免首次配置复杂化。

安全要求：

- API Key 使用 `chrome.storage.local` 保存，永不写入网页 DOM、日志、埋点或导出结果。
- 内容脚本只负责页面解析和 UI；请求由扩展 Service Worker 发起，减少页面脚本接触密钥的机会。
- 设置页提供“显示/隐藏”“测试连接”“删除密钥”“清除全部本地数据”。
- 明示：本地存储不是硬件级保险箱；共享电脑用户建议会话后删除密钥。
- `host_permissions` 仅声明已启用的官方 API 域名；自定义域名需用户单独确认授权。

## 7. 估价输入与输出

### 7.1 页面输入

- 页面 URL、商品 ID（若页面可稳定解析）
- 标题、描述、挂牌价、运费、类目
- 品牌、型号、规格、成色、功能状态
- 卖家公开信息摘要（只取估价所需字段）
- 商品图片：V1 默认只传文本和图片数量；用户开启“允许发送图片”后才传图片
- 页面可见的浏览/想要等弱信号

解析失败时允许用户在侧栏中补充或更正型号、成色与配件。插件必须向用户展示“将发送给模型的内容摘要”。

### 7.2 结构化输出

模型必须按 JSON Schema 返回，至少包含：

- `suggestedPrice`：建议成交价
- `priceRange.min/max`：合理区间
- `confidence`：高 / 中 / 低
- `verdict`：偏高 / 合理 / 偏低 / 信息不足
- `factors[]`：影响价格的关键因素及方向
- `risks[]`：描述中的风险与缺失信息
- `questions[]`：建议向卖家确认的问题
- `negotiationText`：可复制的议价话术
- `disclaimer`：结果边界

结果校验失败时自动进行一次本地结构修复；仍失败则展示可读文本，同时提示“结果格式异常”，不得显示虚假的精确数值。

## 8. 核心交互与状态

### 8.1 结果层级

第一屏只回答三个问题：

1. 建议多少钱：大号建议价。
2. 合理范围是多少：区间 + 页面挂牌价位置。
3. 为什么：最多 3 条最强依据。

风险、待确认问题和议价话术放在折叠区域，减少信息噪音。

### 8.2 系统状态

- 未配置：展示配置引导，不出现不可用的估价按钮。
- 已配置未估价：展示商品摘要和主按钮。
- 分析中：按钮禁用，可取消；分阶段提示，超过 12 秒显示“仍在分析”。
- 成功：展示结果并记录在当前标签页会话中。
- 商品变化：检测 URL 或商品 ID 变化后清除旧结果，避免串单。
- 配额/余额不足：保留商品摘要与输入，提供“切换提供商”。
- 网络失败：提供重试，不重复扣费；是否真正计费以提供商规则为准。
- 页面解析失败：允许手动输入，提供反馈入口。

### 8.3 防误操作

- 任何可能产生模型费用的动作都由用户明确点击触发。
- 切换模型、展开设置、复制文本不发起请求。
- “清除密钥”需要二次确认；关闭侧栏不删除结果。
- 不自动点击或覆盖闲鱼原有控件。

## 9. 视觉设计规范

### 9.1 视觉语言

- 主色：闲鱼黄 `#FFE60F`
- 主文字：近黑 `#1F1F1F`
- 价格强调：橙红 `#FF4D00`
- 页面背景：浅灰 `#F5F6F7`
- 卡片背景：白色 `#FFFFFF`
- 次文字：灰 `#8A8A8A`
- 圆角：入口 999px；侧栏 20px；卡片 16px；按钮 14px
- 阴影：只用于浮层与侧栏，卡片以浅灰底或细分隔线为主

### 9.2 Logo

浏览器扩展图标使用黄色圆角方形底 + 黑色“闲”字。建议同时准备 16、32、48、128px 四档 PNG；小尺寸中只保留“闲”字，不加入 AI 等细字。插件名称建议使用“闲值 AI”，并在安装页和侧栏稳定显示“非闲鱼官方”。

### 9.3 与原页面关系

侧栏采用闲鱼黄黑语言，但不复制闲鱼官方 Logo 组合或伪造官方导航。原页面仍是视觉主体；扩展入口、侧栏标题与免责声明清楚标注第三方 AI 助手身份。

## 10. 纯浏览器扩展架构

建议采用 Manifest V3：

- `content-script`：识别支持页面、提取结构化商品数据、挂载入口和侧栏容器。
- `side-panel` 或隔离的 Shadow DOM：渲染 UI，避免与页面 CSS 冲突。
- `service-worker`：读取本地配置、请求模型、统一超时和错误映射。
- `options-page`：管理提供商、模型、隐私与本地数据。
- `storage`：提供商配置与用户偏好使用 `chrome.storage.local`；当前商品结果优先使用内存或 `chrome.storage.session`。

最小权限建议：`storage`、`activeTab`、`scripting`；闲鱼页面匹配与模型域名使用精确 `host_permissions`。禁止使用 `<all_urls>` 作为默认权限。

## 11. 隐私、合规与风险

1. 首次请求前展示数据去向：商品内容将直接发送至用户选择的模型提供商。
2. 默认不发送用户名、头像、精确地址等与估价无关信息。
3. 图片发送默认关闭；开启时说明可能包含隐私背景。
4. 不保存完整商品历史；若未来增加历史记录，默认本地、可一键清除。
5. “闲”字图标和闲鱼风格可能引发商标、平台审核或用户混淆风险。发布前需要完成命名与图标合规审查，并保留更换为自有品牌图标的方案。
6. 页面 DOM 可能变动，解析器需要版本化和降级策略。
7. 浏览器直连模型受 CORS、区域网络、接口策略影响；测试连接必须在安装后立即验证。

## 12. 成功指标（本地匿名统计可选）

在“无后端”约束下，V1 默认只做本地计数并允许用户关闭：

- 首次配置成功率 ≥ 85%
- 从打开侧栏到得到结果的完成率 ≥ 75%
- 日常估价中位操作次数 ≤ 2 次
- 可解析商品页比例 ≥ 90%
- 模型错误可被明确归因的比例 ≥ 95%

不采集商品正文、API Key、模型原始回复或用户聊天内容。

## 13. V1 验收标准

- Chrome / Edge 的闲鱼商品详情页可正常显示入口与侧栏。
- 四个默认提供商均可独立配置、测试、切换和删除密钥。
- 切换提供商不会自动发起请求；估价动作有明确费用提示。
- 商品标题、描述、价格、规格的解析准确率达到测试样本要求。
- 估价结果包含建议价、区间、置信度、依据、风险和免责声明。
- 侧栏不遮挡核心购买操作，支持键盘操作、Esc 关闭与焦点返回。
- API Key 不出现在 DOM、控制台、错误上报和剪贴板中。
- 中文页面、配置与错误提示无乱码，所有项目文件保持 UTF-8 with BOM。

## 14. 版本路线

### V0.1 设计验证

- 完成当前原型中的入口、结果、设置和首次配置交互。
- 用 10–20 个典型商品页验证信息层级与解析字段。

### V1.0 最小可用版本

- 四提供商接入、文本估价、风险提示、议价话术、设置页。
- 完成本地安全检查、权限收敛、异常映射与商店审核材料。

### V1.1 增强

- 用户主动开启的图片理解。
- 卖家模式：挂牌策略与文案优化。
- 用户手动提供同类商品样本后的对比估价。

## 15. 原型验证重点

本轮原型优先验证：入口是否与闲鱼页面融合、侧栏是否遮挡核心操作、首屏是否快速回答价格问题、提供商切换与 API Key 配置是否足够清晰，以及“第三方 AI 建议”的边界是否醒目但不过度打扰。
